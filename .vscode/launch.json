{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Tools",
            "file": "${workspaceFolder}/docs/index.html",
            "webRoot": "${workspaceFolder}"
        }
    ]
}